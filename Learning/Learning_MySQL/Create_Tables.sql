CREATE TABLE `DnaE_sequences`.`Actinobacteria_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Bacteroidetes_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Chlamydiae_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Cyanobacteria_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Fermicutes_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Fusobacteria_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Spirochaetes_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Tenericutes_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`Thermotogae_etal_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));
CREATE TABLE `DnaE_sequences`.`PolC_sequences` (
  `Header` VARCHAR(100) NOT NULL DEFAULT 'Empty',
  `Sequence` VARCHAR(3000) NOT NULL DEFAULT 'A',
  PRIMARY KEY (`Header`));